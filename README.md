# Agent SDK Comparison
使用了3个不同的SDK，比较不同SDK适用场景。
# 项目结构
OpenAI Agents SDK -> customer_service_OpenAI_Agents_SDK
LangGraph -> email_approval_LangGraph
CrewAI -> researcher_writer_CrewAI
# 使用说明
不同SDK库依赖可能有冲突，建议各自存放在虚拟环境中。
同目录下创建.env放置API_KEY和BASE_URL，或是自行设置环境变量。

